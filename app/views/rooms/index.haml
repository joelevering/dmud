%h1 Rooms

= link_to "New Room", new_room_path

- if @rooms.present?
  %table
    %th Name
    %th Description
    - @rooms.each do |room|
      %tr
        %td= link_to room.name, room_path(room)
        %td= room.description
- else
  No Rooms D:
